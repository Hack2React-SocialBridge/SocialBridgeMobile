import { FlatList, View, Text } from "react-native";
import Wrapper from "../components/Wrapper";

function Header() {
  <View style={{ width: '100%', }}></View>
}


export default function Home() {
  return <Wrapper style={{backgroundColor: '#ff0333'}}>
  </Wrapper>
}